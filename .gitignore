node_modules
dist
.env
docker/.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.pem
docs/
scripts/
**/package-lock.json

.git_original_during_wip
.aider*
data
deploy.sh
CLAUDE.md

# Downloaded model files (keep the script and .gitkeep)
fallback_models/*/
deploy.sh

# Downloaded dictation regex rules (fetched at runtime)
app/ui/public/dictation-regex/

framagit_murmure
TODO.md

